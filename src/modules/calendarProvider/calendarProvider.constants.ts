import type { CalendarProviderName } from '@/modules/calendarProvider/calendarProvider.types';

export const calendarProviderNameMapper: Record<CalendarProviderName, string> = {
  GOOGLE: 'Google',
};
